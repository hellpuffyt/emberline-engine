**What changed and why**

**How it was tested** (which suite, which new test)

**Save-file format changed?** no / yes — save `version` bumped and CHANGELOG updated

**Benchmark before/after** (only for `timeline.ts` / `physics.ts` changes)

- [ ] `npm test`, `npm run lint` and `npm run headless` pass
- [ ] No runtime dependencies; components stay JSON
